@section('topbar_title', 'Hasil ' . ($isPractice ? 'Latihan' : 'Kuis') . ' Peserta — ' . $quiz->title)
<x-app-layout><div class="max-w-6xl mx-auto space-y-6"><div><a href="{{ route($isPractice ? 'practices.index' : 'quizzes.index', $course) }}" class="inline-flex text-xs font-bold text-slate-500 mb-2">← Kembali ke Manajemen {{ $isPractice ? 'Latihan' : 'Quiz' }}</a><h1 class="text-xl font-bold text-slate-800">Hasil {{ $isPractice ? 'Latihan' : 'Kuis' }} Peserta</h1><p class="text-xs text-slate-500 mt-1">{{ $isPractice ? 'Latihan' : 'Quiz' }}: <span class="font-semibold text-blue-600">{{ $quiz->title }}</span>@unless($isPractice) · Nilai lulus: {{ $quiz->passing_score }}%@endunless</p></div>
<div class="bg-white rounded-2xl border border-slate-100 shadow-sm overflow-hidden"><div class="overflow-x-auto"><table class="w-full text-left text-sm"><thead><tr class="bg-slate-50 border-b text-slate-400 text-xs uppercase"><th class="px-6 py-4">Peserta</th><th class="px-6 py-4 text-center">Percobaan</th><th class="px-6 py-4">Waktu Pengerjaan</th><th class="px-6 py-4 text-center">Skor / Nilai</th><th class="px-6 py-4 text-center">Status</th><th class="px-6 py-4 text-center">Aksi</th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($attempts as $attempt)<tr><td class="px-6 py-4"><div class="font-semibold">{{ $attempt->user->name }}</div><div class="text-xs text-slate-400">{{ $attempt->user->email }}</div></td><td class="px-6 py-4 text-center">{{ $attempt->attempt_number }}</td><td class="px-6 py-4 text-xs">Mulai: {{ $attempt->started_at->timezone('Asia/Jakarta')->format('d M Y H:i') }}@if($attempt->submitted_at)<br>Selesai: {{ $attempt->submitted_at->timezone('Asia/Jakarta')->format('d M Y H:i') }}<br><span class="text-slate-400">Durasi: {{ $attempt->getDurationLabel() }}</span>@else<br><span class="text-amber-600 font-semibold">Sedang dikerjakan</span>@endif</td><td class="px-6 py-4 text-center font-bold {{ $attempt->submitted_at ? ($isPractice || $attempt->is_passed ? 'text-emerald-600' : 'text-red-500') : 'text-slate-400' }}">{{ $attempt->submitted_at ? number_format($attempt->score, 0) . '%' : '-' }}</td><td class="px-6 py-4 text-center">@if(!$attempt->submitted_at)<span class="px-2.5 py-1 rounded-full text-xs font-bold bg-amber-50 text-amber-700">BERJALAN</span>@elseif($isPractice)<span class="px-2.5 py-1 rounded-full text-xs font-bold bg-violet-50 text-violet-700">SELESAI</span>@else<span class="px-2.5 py-1 rounded-full text-xs font-bold {{ $attempt->is_passed ? 'bg-emerald-50 text-emerald-700' : 'bg-red-50 text-red-600' }}">{{ $attempt->is_passed ? 'LULUS' : 'GAGAL' }}</span>@endif</td><td class="px-6 py-4 text-center"><form action="{{ route($isPractice ? 'practices.attempts.destroy' : 'quizzes.attempts.destroy', [$course, $quiz, $attempt]) }}" method="POST" onsubmit="return confirm('Reset percobaan ini?')">@csrf @method('DELETE')<button class="px-3 py-1.5 bg-red-50 text-red-600 text-xs font-bold rounded-lg border border-red-200">Reset Percobaan</button></form></td></tr>@empty<tr><td colspan="6" class="px-6 py-12 text-center text-slate-400">Belum ada peserta yang mengerjakan {{ $isPractice ? 'latihan' : 'kuis' }} ini.</td></tr>@endforelse</tbody></table></div></div></div></x-app-layout>
