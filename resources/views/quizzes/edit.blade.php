@section('topbar_title', 'Edit ' . ($isPractice ? 'Latihan' : 'Quiz') . ' — ' . $quiz->title)
<x-app-layout><div class="max-w-6xl mx-auto space-y-6"><div><a href="{{ route($isPractice ? 'practices.index' : 'quizzes.index', $course) }}" class="inline-flex text-xs font-bold text-slate-500 mb-2">← Kembali ke Daftar {{ $isPractice ? 'Latihan' : 'Quiz' }}</a><h1 class="text-xl font-bold text-slate-800">Edit {{ $isPractice ? 'Latihan' : 'Quiz' }}</h1><p class="text-xs text-slate-500">{{ $isPractice ? 'Latihan chapter, tanpa timer dan nilai kelulusan.' : ($quiz->isFinalQuiz() ? 'Final Quiz' : 'Chapter Quiz') }}</p></div>
@if(session('success'))<div class="bg-emerald-50 border border-emerald-200 text-emerald-700 text-sm px-4 py-3 rounded-xl">{{ session('success') }}</div>@endif
<div class="grid grid-cols-1 lg:grid-cols-2 gap-6"><form action="{{ route($isPractice ? 'practices.update' : 'quizzes.update', [$course, $quiz]) }}" method="POST" class="bg-white rounded-2xl border border-slate-100 shadow-sm p-6 space-y-4">@csrf @method('PUT')<h2 class="font-bold">Konfigurasi {{ $isPractice ? 'Latihan' : 'Quiz' }}</h2><div><label class="block text-xs font-semibold mb-1">Judul {{ $isPractice ? 'Latihan' : 'Quiz' }} *</label><input name="title" required value="{{ old('title', $quiz->title) }}" class="w-full border border-slate-200 rounded-xl px-3 py-2.5 text-sm"></div><div><label class="block text-xs font-semibold mb-1">Deskripsi</label><textarea name="description" rows="2" class="w-full border border-slate-200 rounded-xl px-3 py-2.5 text-sm">{{ old('description', $quiz->description) }}</textarea></div>
@if($isPractice)<input type="hidden" name="review_policy" value="show_all"><div><label class="block text-xs font-semibold mb-1">Chapter *</label><select name="chapter_id" required class="w-full border border-slate-200 rounded-xl px-3 py-2.5 text-sm">@foreach($chapters as $chapter)<option value="{{ $chapter->id }}" @selected(old('chapter_id', $quiz->chapter_id) == $chapter->id)>{{ $chapter->order }}. {{ $chapter->title }}</option>@endforeach</select></div><div><label class="block text-xs font-semibold mb-1">Maks. percobaan <span class="text-slate-400">(kosong = tanpa batas)</span></label><input type="number" name="max_attempts" min="1" max="100" value="{{ old('max_attempts', $quiz->max_attempts) }}" class="w-full border border-slate-200 rounded-xl px-3 py-2.5 text-sm"></div>@else<div><label class="block text-xs font-semibold mb-1">Chapter <span class="text-slate-400">(kosong = Final Quiz)</span></label><select name="chapter_id" class="w-full border border-slate-200 rounded-xl px-3 py-2.5 text-sm"><option value="">Final Quiz</option>@foreach($chapters as $chapter)<option value="{{ $chapter->id }}" @selected(old('chapter_id', $quiz->chapter_id) == $chapter->id)>{{ $chapter->order }}. {{ $chapter->title }}</option>@endforeach</select></div><div class="grid grid-cols-2 gap-3"><div><label class="block text-xs font-semibold mb-1">Timer (menit)</label><input type="number" name="time_limit" min="1" max="300" value="{{ old('time_limit', $quiz->time_limit) }}" class="w-full border rounded-xl px-3 py-2.5 text-sm"></div><div><label class="block text-xs font-semibold mb-1">Nilai lulus *</label><input type="number" name="passing_score" required min="1" max="100" value="{{ old('passing_score', $quiz->passing_score) }}" class="w-full border rounded-xl px-3 py-2.5 text-sm"></div></div><div><label class="block text-xs font-semibold mb-1">Maks. percobaan *</label><input type="number" name="max_attempts" required min="1" max="10" value="{{ old('max_attempts', $quiz->max_attempts) }}" class="w-full border rounded-xl px-3 py-2.5 text-sm"></div><div><label class="block text-xs font-semibold mb-1">Kebijakan hasil</label><select name="review_policy" class="w-full border rounded-xl px-3 py-2.5 text-sm"><option value="show_all" @selected($quiz->review_policy === 'show_all')>Tampilkan semua</option><option value="points_only" @selected($quiz->review_policy === 'points_only')>Skor saja</option><option value="hide_all" @selected($quiz->review_policy === 'hide_all')>Sembunyikan detail</option></select></div>@endif
<div class="flex gap-4 text-xs"><label><input type="checkbox" name="shuffle_questions" value="1" @checked($quiz->shuffle_questions)> Acak soal</label><label><input type="checkbox" name="shuffle_options" value="1" @checked($quiz->shuffle_options)> Acak pilihan</label><label><input type="checkbox" name="is_active" value="1" @checked($quiz->is_active)> Aktif</label></div><button class="w-full bg-blue-600 text-white py-2.5 rounded-xl text-sm font-bold">Simpan Perubahan</button></form>
<div class="bg-white rounded-2xl border border-slate-100 shadow-sm p-6"><div class="flex justify-between mb-4"><div><h2 class="font-bold">Soal dalam {{ $isPractice ? 'Latihan' : 'Quiz' }}</h2><p class="text-xs text-slate-500">Pilih dari bank soal {{ $quiz->isFinalQuiz() ? 'seluruh course' : 'chapter terkait' }}.</p></div><span class="text-xs px-2 py-1 bg-blue-100 text-blue-700 rounded-full">{{ count($selectedQuestionIds) }} dipilih</span></div><form action="{{ route($isPractice ? 'practices.questions.sync' : 'quizzes.questions.sync', [$course, $quiz]) }}" method="POST" class="space-y-2">@csrf<div class="space-y-2 max-h-96 overflow-y-auto">@forelse($availableQuestions as $question)<label class="flex gap-3 p-3 border rounded-xl cursor-pointer"><input type="checkbox" name="question_ids[]" value="{{ $question->id }}" @checked(in_array($question->id, $selectedQuestionIds))><div><span class="text-xs font-semibold">{{ $question->type_label }}</span><p class="text-xs text-slate-700 mt-1">{{ $question->question_text }}</p></div></label>@empty<div class="text-center py-8 text-sm text-slate-400">Belum ada soal pada bank soal ini.</div>@endforelse</div><button class="w-full py-2.5 bg-emerald-600 text-white text-sm font-bold rounded-xl">Simpan Pilihan Soal</button></form></div></div></div></x-app-layout>
